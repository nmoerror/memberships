import React from 'react';
import { View, Text, Animated } from 'react-native';
import styled from 'styled-components';

const Statistics = () => {
  return (
    <Section>
      <Text>askjndasknj</Text>
    </Section>
  );
};

const Section = styled.View`
  margin-top: 10px;
`;

export default Statistics;
